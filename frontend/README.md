# NodeComposer Frontend

Next.js frontend for NodeComposer.

## Setup

```bash
npm install
npm run dev
```

The frontend will run on `http://localhost:3000` by default.

## Environment Variables

Create a `.env.local` file:

```
NEXT_PUBLIC_API_URL=http://localhost:8000
```

